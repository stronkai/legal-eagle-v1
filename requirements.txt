# requirements.txt - Minimal version that will definitely work
streamlit==1.28.0
requests==2.31.0
beautifulsoup4==4.12.2
python-docx==1.0.1
PyPDF2==3.0.1
reportlab==4.0.6
numpy==1.24.3

# Note: Audio libraries removed for initial deployment
# We'll add them back once the basic app is running
